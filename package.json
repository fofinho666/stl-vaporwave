{
  "name": "stl-vaporwave",
  "scripts": {
    "dev": "concurrently npm:dev:backend npm:dev:frontend -n 'Go,React' -c 'cyan.bold,blue.bold'",
    "dev:backend": "cd backend && make dev",
    "dev:frontend": "cd frontend && npm dev"
  },
  "devDependencies": {
    "concurrently": "^5.3.0"
  },
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  }
}
